package com.mikey.aop.sorting.threading;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ThreadNodeTest {

/*    ThreadNode threadNode;
    int testData;

    public ThreadNodeTest() {
        Runnable r = () -> testData = 10;
        threadNode = new ThreadNode(r*//*, 1000*//*);
    }

    @Test
    void getRunnable() {
        assertEquals(0, testData);
        Thread t = new Thread(threadNode.getRunnable());
        t.start();
        try {t.join();}catch (Exception e){System.out.println(e);}
        assertEquals(10, this.testData);
    }*/

}